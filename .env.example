# TikTok CDN Demo Configuration
# Copy this file to .env if needed

# Server Configuration (optional)
HOST=0.0.0.0
PORT=8000

# CDN Configuration (optional - can be set via web interface)
CDN_UPLOAD_URL=https://ads.tiktok.com/api/v2/i18n/material/image/upload/

# Note: CDN session cookies are now configured via the web interface
